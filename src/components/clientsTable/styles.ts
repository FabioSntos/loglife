import styled from 'styled-components'

export const Container = styled.div`

  table {
    width: 100%;
    border-spacing: 0 0.5rem;

    th {
      font-size: 14px;
      color: var(--text-body);
      font-weight: 400;
      padding: 1rem 1rem;
      text-align: left;
      line-height: 1.5 rem;
    }
  }
`